import MuiCssBaseline from '@mui/material/CssBaseline';
import React from 'react';

const CssBaseline = () => {
  return <MuiCssBaseline enableColorScheme />;
};

export default React.memo(CssBaseline);
